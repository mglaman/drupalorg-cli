# drupalorg-cli Agent Skill

The skill file has moved to [`skills/drupalorg-cli/SKILL.md`](skills/drupalorg-cli/SKILL.md).

To install the skill into your project, run:

```bash
drupalorg skill:install
```

This copies `SKILL.md` and the `references/` workflow guides into
`.claude/skills/drupalorg-cli/` (or a custom `--path`).
